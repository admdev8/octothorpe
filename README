My own C library.

Tested on MinGW x86/x64, Linux x86/x64, Mac OS X x64 (once in a while).

Requires C11 compiler, e.g. GCC >= 4.7.

* Issues:

While compiling for Windows 2000, set TARGET_IS_WINDOWS_2000.

-- dennis(a)yurichev.com
